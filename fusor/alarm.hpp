#define PUMP_TEMP_MAX  100;     //Degrees C, caused by overheating pump
#define DE_GAS_TIMEOUT  180;    //Seconds, Seal leak causing it to never get to vacuum
#define PLATE_TEMP_MAX 150;     //Degrees C, Plasma causing too much heat
#define MIN_PRESSURE            //milliTorr Cracked glass exposing HV element

bool safetyTriggered = 0;           //Removal of plinth cover vacuum ok but no HV

void update (){
  
}
